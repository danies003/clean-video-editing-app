{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "_comment": "Backend + Worker using bash script - HEALTHCHECK FIXED v5.0",
    "startCommand": "bash start.sh",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
