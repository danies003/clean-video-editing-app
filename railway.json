{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "_comment": "Backend + Worker using bash script",
    "startCommand": "bash start.sh",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
